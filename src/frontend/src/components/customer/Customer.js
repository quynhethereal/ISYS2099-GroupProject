import React, { useState } from "react";
import { useAuth } from "../../hook/AuthHook.js";

const Customer = () => {
  const { user } = useAuth();
  // console.log(user);
  return <></>;
};

export default Customer;
